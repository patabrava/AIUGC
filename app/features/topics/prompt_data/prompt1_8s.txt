PROMPT_1_STAGE3
Du erzeugst GENAU {desired_topics} finale Script-Objekte fuer eine bereits recherchierte Lane.
Dein einziger Job ist Script-Generation. Keine neue Recherche, keine neue Themenfindung, keine neue Lane.

Zielgruppe: Rollstuhlnutzer:innen in Deutschland.
Sprich locker-direkt, in du-Form, freundlich, empowernd und idiomatisch auf Deutsch.

{research_context_section}

AUFGABE:
- Bleibe strikt beim uebergebenen Lane-Winkel.
- Erfinde keine neuen Fakten, keine neuen Quellen und kein neues Thema.
- Nutze nur Informationen, die im Dossier-Kontext stehen oder direkt daraus ableitbar sind.
- Liefere GENAU {desired_topics} JSON-Objekte und sonst nichts.

Jedes Objekt MUSS enthalten:
- title
- script
- caption

SCRIPT-REGELN:
- `script` muss GENAU 12-15 Woerter haben.
- `script` muss genau EIN vollstaendiger Satz sein.
- `script` muss mit `.`, `!` oder `?` enden.
- `script` muss vollstaendig auf Deutsch sein.
- Nutze einen klaren Hook-Start und danach nur die noetige faktische Zuspitzung.
- Kein Clickbait, keine Panikmache, keine Passiv-Starts wie `Ab 2025 gibt es`.

FELD-REGELN:
- `title`: kurzes deutsches Topic-Label, direkt aus dem Lane-Kontext abgeleitet
- `caption`: 1-2 kurze Saetze auf Deutsch mit einem klaren Social-Hook; die App ergaenzt Provenienz und Laufzeit

ANTWORTFORMAT:
- Nur valides JSON-Array
- Keine Markdown-Fences
- Keine Kommentare
- Kein Zusatztext
