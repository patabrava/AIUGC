# AGENTS.md

0) Non-negotiable Locality Envelope (default)
- Vanilla-first: prefer platform primitives; default to vanilla first, and only use frameworks if justified.
- Dependency budget: 0 by default; max 1-2 per concern, only if justified.
- File budget: Fewer is better. Create as fewer files as possible to achieve production-grade quality without creating context overwhelm (zero unnecessary files).
- LOC budget: target <= 1000 LOC/file; split when > 2000 LOC. Avoid context rot growth.
- Every Plan offered must explicitly state: {files, LOC/file, deps}.

1) Role routing (global)
- Keep bridgecode playbooks in `bridgecode/` and generated artifacts in `agents/`; do not mix the two folders.
- When doing backend development always follow the LLM_FRIENDLY_ENGINEERING_BACKEND instructions.
- When doing frontend design always follow the LLM_FRIENDLY_ENGINEERING_FRONTEND instructions.
- When planning or debugging always follow the LLM_FRIENDLY_PLAN_TEST_DEBUG instructions.
- Whenever starting a conversation (no previous turns detected) check only one time, to start the conversation, the `agents/canon.md` file to get an overview of the repo you're working with.
  - If `agents/canon.md` does not exist, instruct the user to use `LIRA` and the sub-role `senior`.
- Whenever `EYE` fixes an error, through simple implementation or isolated debugging (with or without loop-breaker), add a compoacted, dense one-line with the solution to the error (to be prevented) in the Specific repo rules within the `/AGENTS.md` file.
  - If a correction for an error already exist, either modify that one-line (if the correction was insufficient) or add extra instructions (if new related errors emerged but the initial correction was correct).
- Codex bridgecode routing:
  - If the user invokes `INSTRUCT`, immediately read `bridgecode/INSTRUCT.md` and follow it. When that playbook routes to negentropy behavior, read `bridgecode/negentropy.md`.
  - If the user invokes `LIRA` for a new project or architecture definition, read `bridgecode/LIRA.md` and `bridgecode/architect.md`.
  - If the user invokes `LIRA` for an existing codebase review or asks to audit/review the repo, read `bridgecode/LIRA.md` and `bridgecode/senior.md`.
  - If the user invokes `EYE`, or asks to implement/debug work using bridgecode execution, read `bridgecode/EYE.md` and `bridgecode/plan-code-debug.md`.
- `bridgecode/` contains the Codex playbooks. `agents/` is reserved for generated artifacts such as `canon.md`, `plan.md`, `review.md`, `negentropized_instructions.md`, and `agents/testscripts/failure_report.md`.

LLM_FRIENDLY_ENGINEERING_BACKEND
	"""
	Backend coding: When building software, organize all code by feature into vertical slices where interface, logic, schema, validation, and tests live together in immediate proximity—never scatter related concerns across distant directories or technical layers. Default to language primitives and standard libraries (vanilla-first mandate) before introducing any external dependency, and when dependencies prove necessary, assign exactly one tool per architectural concern, wrapping specialized libraries in thin adapter interfaces that isolate external complexity at system edges. Keep files purposeful and focused, naming them with boring descriptive terminology, and promote code to shared locations only after the rule of three (genuine reuse appears three times) proves the pattern stable. Define explicit schema-validated contracts at every feature boundary, validating all inputs at entry and shaping all outputs at exit using uniform error models that include status, code, message, and optional details without leaking implementation specifics. Confine singletons (database clients, cache clients, auth providers, config loaders, logging systems) strictly to infrastructure zones as stateless factories, never embedding domain logic within them. Limit build operations to essential commands (install, develop, test, build, start) with pinned dependencies ensuring deterministic reproducible builds, and implement structured feature-scoped logging with request correlation for operational transparency. Write unit tests for key pure functions, end-to-end tests for critical paths, and contract tests for boundary schemas, co-locating all tests with the code they validate. Target established accessibility compliance standards with keyboard-first interaction, visible focus states, reduced motion respect, and appropriate ARIA roles, while centralizing design tokens (color, spacing, typography, radius, motion) in exactly one style system per project. Begin architecture as a simple monolith of vertical slices, extracting async work to background jobs only when genuine need arises, creating shared packages only after cross-feature reuse stabilizes, and splitting into services only when isolation, scaling, or compliance boundaries are demonstrated through measured metrics. When refactoring, inline logic toward callers before extracting abstractions, consolidate fragmented files into coherent feature folders, and write or update contracts before changing implementation—replacing unnecessary libraries with vanilla implementations and removing abstractions that no longer provide value. Reject utility collections mixing unrelated concerns, code fragmentation into many tiny files increasing navigation cost, duplicate tools serving identical purposes, implicit global state, reflection-heavy patterns obscuring behavior, and any framework with sparse documentation on critical paths. Ensure every deliverable constitutes a self-contained feature slice with adjacent schema and tests, confining global concerns to singleton adapters without domain logic, providing run instructions fitting three lines producing deterministic results, and documenting selected tools while verifying they meet documentation and predictability thresholds. Present options briefly with clear default recommendations, state assumptions explicitly and proceed without unnecessary blocking, produce small copyable immediately-runnable artifacts, prefer diagrams and checklists before implementation code, generate code in vertical slices rather than global sweeps, refactor incrementally while preserving existing contracts, and ensure every artifact reaches completeness enabling immediate utility. Measure success by minimizing files and directories touched per feature change, maintaining file sizes within productive ranges, achieving high boundary coverage with explicit schemas, keeping local startup and hot-reload times minimal, referencing canonical documentation sources, maintaining high API predictability ratings, providing runnable copy-pasteable snippets for common operations, and maximizing the ratio of vanilla code paths to dependency-heavy paths—where ultimate validation asks whether a competent reader (human or AI agent with LLM capabilities performing code generation and modification tasks) can understand, modify, and extend the system correctly on first encounter without external context, preferring locality over abstraction, explicitness over cleverness, and simplicity over sophistication in every decision.
	"""
END_LLM_FRIENDLY_ENGINEERING_BACKEND

LLM_FRIENDLY_ENGINEERING_FRONTEND
	"""
	Frontend design: When designing frontend interfaces, first identify and explicitly blacklist all high-probability solutions (Inter/Roboto/Open-Sans/Arial/system-fonts, purple-white gradients, centered hero layouts with stock imagery, evenly-distributed safe palettes, uniform spacing rhythms, generic card grids, predictable button hierarchies, anything describable as "clean-and-modern") treating statistical convergence as failure signal rather than safety indicator. After eliminating the obvious, navigate toward latent space edges where distinctive memorable solutions exist by committing fully to a singular aesthetic stance combining typography (font-choice defining voice and hierarchy through unconventional pairings or single-typeface systems with extreme weight contrast), color (palettes rejecting safety through unexpected combinations, restricted ranges, or intentional dissonance that creates visual tension), spatial composition (asymmetry, unexpected density variations, deliberate negative space manipulation, rhythm breaks that demand attention), and motion (purposeful animation revealing state changes, guiding attention, or reinforcing brand character without decorative excess). Every choice must satisfy the defensibility test: solve the actual problem equally-well or better while being categorically non-generic, meaning if the output would blend invisibly into a corpus of similar interfaces reject and regenerate, ensuring the result remains immediately recognizable as deliberately designed rather than template-generated. Maintain accessibility requirements (keyboard navigation, focus states, ARIA semantics, color-contrast ratios, reduced-motion respect) as non-negotiable constraints within which creative decisions operate, and ensure all interactive elements provide clear affordances through visual distinction rather than convention-mimicry. Organize component architecture by feature-locality principles where styling logic lives adjacent to component implementation, centralizing design tokens (but rejecting token systems that enforce homogeneity) and documenting aesthetic rationale inline so future modifications preserve intentional distinctiveness. Test every visual decision against recognition criteria: would a user remember this interface after brief exposure, does it communicate brand or purpose through design choices rather than explicit messaging, and does it resist the gravitational pull toward generic-pleasant mediocrity that dominates training distributions.
	"""
END_FRIENDLY_ENGINEERING_FRONTEND

LLM_FRIENDLY_PLAN_TEST_DEBUG
	"""
	When planning software with LLM-teams and human operators, structure all work into a single implementation-block (the full application delivered end-to-end in one long-shot pass), with an explicit capability map and debug-scopes for localized diagnosis, followed by testscripts executed in one-shot to validate, debug, iterate, and polish the whole implementation and any subsequent local fixes. Begin every engagement by capturing the complete environment matrix (operating-system, runtime-versions, tool-versions, build-identifiers, configuration-flags) and non-functional requirements (latency-targets, memory-ceilings, reliability-thresholds, security-boundaries) as Context-Zero, then produce an implementation-block plan with explicit capability coverage, dependency map, boundary map, and pass-fail criteria, where the block includes its deliverable scope, testscript structure (identifier, objective, prerequisites, setup-steps, run-commands, expected-observations at each boundary, artifact-capture points with formats, cleanup-procedures, known-limitations), and observation checklist (environment-details, exact-steps-executed, observed-versus-expected comparisons, artifact-paths-and-timestamps, reproducibility-frequency), and where any internal decomposition is used for reasoning and debugging in a single implementation-block. Execute the implementation-block one-shot by implementing the full application end-to-end with structured-logging at boundaries and error-paths (using standard-error-envelopes containing status-code-message-context), explicit-input-validation, deterministic one-command-setup with pinned-versions, and instrumentation that surfaces correlation-identifiers across all boundaries—delivering immediately-runnable code that makes cold-start, execution, and teardown reproducible without hidden-behavior or silent-failures. After one-shot completion, execute testscripts one-shot in the real runtime environment (browser, server, device, full-stack) rather than detached-unit-scaffolds, where each testscript runs the actual-application with whole-system-behavior-verification, re-executing all previously-passing testscript groups after each meaningful fix or subsystem-level change to detect regressions, ensuring no implementation is accepted without satisfying current criteria and confirming no collateral damage to previously-passing validations. When failures occur during testscript execution, create minimal-reproducers without altering semantics, instrument observation-points before editing code, form one-hypothesis and change one-variable per iteration, classify defects to a specific debug-scope or boundary using systematic root-cause categorization (environment-mismatch, dependency-drift, configuration-gaps, contract-mismatches, stateful-side-effects, timing-races, resource-limits, filesystem-semantics, network-factors, clock-timeouts, data-corruption, test-production-divergence), apply the smallest-most-local-fix possible, prove fixes with reproducer-then-full-testscript, and add regression-checks to prevent recurrence—issuing minimal-targeted-data-requests (specifying exact-artifact-locations, formats, collection-commands) rather than broad-fishing-expeditions, stopping after two-failed-attempts to generate a failure report, request new-specific-observations and reformulate-hypotheses instead of blind-retries. Define clear role-separation where LLM-teams produce immediately-runnable plans and code with complete testscripts and observation-checklists, own triage-hypothesis-fix-regression-creation, emit structured-errors and actionable-diagnostics by default, and issue minimal-targeted-requests when evidence insufficient, while human-operators execute specified-testscripts faithfully, capture-and-attach requested-artifacts precisely at collection-points, confirm environment-details and note deviations, follow checklists without deviation-or-interpretation, and avoid speculative-debugging or root-cause-guessing. Implement progressive-validation-depth covering smoke-level (boot, wiring, signal-visibility), happy-path (nominal-journeys with contract-verification), edge-cases (boundary-values and unusual-supported-inputs), failure-paths (meaningful-errors and fallbacks), recovery (reset-retry-idempotency-degradation), budget (latency-throughput-memory-resource-limits), and security (authentication-authorization-input-hardening), never skipping fundamental-levels for advanced-ones or considering happy-path-alone sufficient for production-readiness. Consolidate all passing-testscripts into persistent-regression-suites integrated into continuous-integration or scheduled-execution, adding assertions from every passing-test and creating new-checks from every-failure, prioritizing flaky-test-stabilization as critical-work, and maintaining documented pass-rate-thresholds with execution-time-budgets appropriate to project-scale. Redact secrets, personal-data, and tokens at all collection-points, provide explicit safe-sharing-guidance for artifacts, respect least-privilege when reproducing defects, and never expose sensitive-data in logs-artifacts-or-defect-reports. Structure all defect-reports with required-fields (title, severity, frequency, implementation-block, debug-scope, script-identifier, environment-matrix, build-commit, reproduction-steps, observed-behavior, expected-behavior, artifact-references, suspected-boundary, initial-hypothesis, workaround-if-available, regression-test-status, ownership) sufficient for first-attempt-diagnosis, documenting root-cause with cause-fix-prevention-measures before closure. Keep beginner-defaults simple: single-run-commands for environment-setup, fewer-but-whole-application-tests rather than proliferated-unit-tests, boundary-instrumentation-first with correlation-identifiers, regression-checks-added whenever defects-fixed, and vanilla-implementations that maximize reasoning-clarity over complex-tooling. Measure success by whether a single-guided-run by a non-expert-operator provides enough structured-evidence for definitive-diagnosis-and-fix on first-attempt, validating through metrics including reproducibility-rate on first-observation, time-from-run-start to first-meaningful-signal, convergence-speed after targeted-data-collection, ratio of non-deterministic-failures, completeness of defect-reports with all-required-fields, and protection-coverage of critical-boundaries aligned to prior-incidents. Once a planning engagement is concluded, proceed immediately to one-shot implementation of the implementation-block and its testscripts, ensuring that the full application is immediately runnable, observable, and debuggable without requiring extensive interpretation or multi-step completion work.
	"""
END_LLM_FRIENDLY_PLAN_TEST_DEBUG

2) Specific repo rules
- Live `lippelift.xyz` deploys must run from a VPS checkout plus `/opt/aiugc-prod/.env.production` through `scripts/deploy/production.sh`; do not rely on Hostinger repo-wrapper env injection or repo `.env`, or pushes to `main` will diverge from the live runtime.
- Production Traefik host routing defaults must stay `${TRAEFIK_HOST_RULE:-Host(`lippelift.xyz`)}` in every production-aligned compose file; never default to localhost or live routing will break when VPS env files omit `TRAEFIK_HOST_RULE`.
- Production auth bypass must never trust proxy loopback `request.client.host`; only bypass on localhost-style requested hosts while `ENVIRONMENT=development`, or reverse proxies like Traefik will silently disable login on the live site.
- Topic cron endpoints under `/topics/cron` must bypass the global auth middleware because the route itself validates `Bearer $CRON_SECRET`; otherwise Hostinger scheduler requests get redirected to `/auth/login` before cron auth can run.
- Topic cron wrappers must treat malformed batch rows or transient batch-list failures as non-fatal and report them in `failed_batches`; never let one bad row or query exception turn the whole cron into HTTP 500.
- Topic research selector failures that end with Gemini/LLM returning no usable seeds must fall back to a deterministic non-empty seed list before creating the cron run; never allow an empty seed set to mark the daily cron as failed when the YAML bank still has viable topics.
- Hostinger repo-URL Docker deploys must keep `docker-compose.yaml` and `docker-compose.yml` functionally identical and self-contained (no repo-only `.env` dependency), or the platform will pick the wrong compose entry and boot a broken production stack.
- Hostinger Docker builds must not hide apt/pip inside one opaque `RUN`; split OS-package install and Python dependency install into separate retryable layers or remote build failures become non-diagnostic and stall recovery.
- Runtime dependency pins must stay solver-clean in a fresh Python 3.11 build: `google-genai>=1.3` conflicts with the current Supabase/httpx stack, so keep the SDK on the 1.1/1.2 line unless the full transport set is upgraded together.
- Gemini Interactions accepts `background` Deep Research requests but its follow-up `responseSchema` rejects OpenAI-style `additionalProperties`; strip unsupported schema fields before Gemini JSON generation and log interaction-id/status transitions until completion.
- Async batch background tasks must not run blocking Gemini poll loops on the request event loop; offload long Deep Research waits, and always translate VEO HTTP 429/quota responses into structured `RATE_LIMIT` errors instead of generic 500s.
- In `S2_SEEDED`, treat script review as per-post state in `seed_data`: any script edit resets review to pending, batch advance must require every post marked approved or removed plus at least one approved, and removed posts must be excluded from later prompt/video transitions.
- For PROMPT_1 Deep Research, request the exact first-pass topic count in one interaction per researchable post type, retry transient Gemini poll 429/5xx errors with bounded backoff, and never restart a finished research run just because the downstream normalization/validator step needs repair.
- Topic Deep Research for German audiences must enforce German-only output twice: prompt-level in `prompt1.yaml` and validation-level in `app/features/topics/agents.py`, rejecting and retrying any English or mixed-language `topic/script/source_summary/tone/disclaimer` before persistence.
- PROMPT_1 normalization must defensively repair downstream field issues before validation (default missing `framework`, clip overlong `tone`/`disclaimer`/`source_summary`, etc.) so a completed Deep Research batch never restarts or dies before `create_post_for_batch(...)` and leaves `posts` empty.
- PROMPT_1 synthesized dossier fallbacks must pass through `_normalize_research_dossier_payload(...)` before `ResearchDossier` validation so generated `cluster_id`/text fields are clipped to schema limits and cannot crash post-research persistence.
- PROMPT_1 research parsers must reject top-level topic-list contamination before dossier normalization; if Deep Research returns a list of unrelated topic dicts, synthesize from the seed topic instead of reusing the polluted raw payload or the cron will silently persist zero usable lanes.
- Stage-1 Deep Research must keep one contract only (raw German dossier prose via `prompt1_research.txt`) and PROMPT_1 stage-3 JSON schema must require all item fields (`topic/framework/sources/script/source_summary/estimated_duration_s/tone/disclaimer`) to prevent minimal malformed objects from slipping through retries.
- PROMPT_1 must stay dossier-only: the canonical seed bank comes from `app/features/topics/prompt_data/prompt1_8s.txt`, the research prompt may not carry final script-length constraints, and all script-writing behavior stays isolated in PROMPT_2/script-generation paths.
- Topic discovery dedupe must treat historical `topic_research_dossiers.seed_topic/topic/anchor_topic` as first-class prior coverage and reject globally identical `topic_scripts` per tier, or new cron runs will recycle the same broad seed families while filling the bank with cross-topic script duplicates.
- Mixed batch seeding must scope lifestyle dedupe to the active lane only; never compare lifestyle candidates against same-batch value posts, or cross-lane registry overlap will over-filter the lifestyle pass and strand mixed batches in `S1_SETUP`.
- PROMPT_1 duration-tier parsing must validate terminal punctuation before any normalization and must never keep hard-coded 5-6s/schema bounds once 16s/32s tiers exist, or longer Deep Research scripts will be misclassified and truncated into invalid fragments.
- VEO portrait runs must send REST `parameters.aspectRatio` and `parameters.resolution` explicitly and must not locally reject valid Veo 3.1 `9:16` + `1080p` requests.
- VEO extended chains must never pad or silently cap hops; require at least `base + extension_hops` distinct script segments up front, reject 16s/32s chains that under-segment, and keep the prompt templates aligned to that sentence budget.
- Efficient VEO packed chains must reuse the persisted `video_metadata.veo_segments` verbatim for every extension hop; re-splitting the raw script in the worker duplicates the tail of the 8s base chunk and drops the final segment.
- Segmented VEO prompts must carry only stable identity fields from the saved prompt; reusing the original full-script `action` or final audio block inside packed base/extension hops causes repeated sentences after ~16s and compounds visual drift across later continuations.
- VEO 32s must stay on the legacy `4+7+7+7+7` chain; the efficient `8+7+7+7` route posterizes contrast and facial detail after ~2 hops across unrelated subjects even when segment routing and voice continuity are correct.
- VEO 32s must always resolve to the legacy `4+7+7+7+7` profile even when the efficient-long-route flag is enabled; only the 16s route may use the efficient profile.
- VEO legacy 32s script splitting must keep decimals like `BITV 2.0` intact and greedily pack dialogue so each hop gets a real spoken chunk instead of a tiny tail fragment.
- VEO legacy 32s dialogue splitting must choose the clause boundary closest to the 4s base budget instead of the earliest comma, or the first hop becomes a tiny fragment and the final hop starves.
- VEO talking-head prompts must include an explicit post-dialogue `Ending` instruction that stops speech after the final word and keeps the mouth at rest; generic hold-smile action text alone is insufficient and causes false continuation cuts near clip end.
- VEO talking-head batches must not target full-clip speech: keep upstream scripts around 5-6 spoken seconds inside the current 8-second flow so there is real budget for the end hold and room-tone decay.
- VEO first-frame anchor/image-to-video support must stay disabled on the Gemini Developer preview surface until the API explicitly accepts `image.inlineData`; keep the live handler text-only and gate dormant anchor helpers behind a false default.
- Video submission prompts must collapse all audio guidance into one qualitative `Audio:` block with natural room-tone decay; never send dead-silence requests, RT60/dBFS/LUFS-style metrics, or duplicate audio/post/SFX sections, and Veo exclusions must ship via REST `parameters.negativePrompt` rather than inline `no...` negative text in the main prompt.
- Legacy 32s Veo batches must not inherit the global efficient-long-route experiment bit or random `veo_seed`; only the actual 16s 8+7 efficient profile may stamp `veo_efficient_long_route_enabled=true` and seed metadata, or 32s reruns will diverge from the last stable contract.
- Hostinger `aiugc-prod` must be recreated or restarted whenever `GOOGLE_AI_API_KEY`/`GEMINI_API_KEY` rotate; otherwise the live worker keeps polling Veo operations with a stale project context and returns `403 PERMISSION_DENIED`.
- Hostinger Veo quota bypass flags must be forwarded through both `docker-compose.yaml` and `docker-compose.yml` service environments, and an accepted Veo operation must never be marked failed just because post-submit quota consumption errored; persist the `operation_id` and keep polling.
- When `VEO_DISABLE_LOCAL_QUOTA_GUARD` or `VEO_DISABLE_ALL_QUOTA_CONTROLS` is enabled, video submit handlers must skip quota reservation/consume calls entirely instead of relying on the ledger helpers to no-op, or a stale runtime path can still throw `Quota ledger failed to consume an accepted Veo operation` after the provider already accepted a paid request.
- Batch and single-post VEO submission endpoints must backfill and persist `video_prompt_json` from `seed_data.script` or `seed_data.dialog_script` before submit; otherwise approved posts will be skipped and `generate-all` can silently return zero videos.
- Vertex extension/submission flows must thread an explicit `output_gcs_uri`/`storageUri` from config into both the initial submit and every extension hop; relying on the returned inline payload alone leaves the hub unset and makes the chain non-durable.
- Vertex extension hops must never pass a `data:` URI into `video.gcsUri`; if the completed base clip is inline, stage it into GCS first and then submit the hop against the staged `gs://` object, or Vertex will fail with storage access errors even when bucket IAM is correct.
- Legacy `32s` VEO chains must restore the whole historical request contract, not just hop timing: keep visual negatives inline on base and extension prompts and do not move them to REST `negativePrompt`, or late-hop continuations posterize and drift even on the old `4+7+7+7+7` route.
- PROMPT_2 hook expansions must update `parse_prompt2_response()` in lockstep: recognize every allowed hook-family prefix and split consecutive one-line scripts even without blank separators, or adjacent scripts will merge and corrupt dialog extraction.
- Live seeding feeds must not rely only on pre/post Deep Research stage wrappers; thread Gemini interaction polling callbacks through `generate_gemini_deep_research()` into topic discovery so the feed emits heartbeat/progress events during long research waits instead of appearing live-but-stale.
- Gemini Deep Research live feeds must consume the provider stream with `stream=True` plus `thinking_summaries="auto"` and resume from `last_event_id`; pure status polling only produces repetitive synthetic updates and cannot show real research progress.
- Gemini Deep Research SSE does not emit a constant thought stream: handle `interaction.status_update`, use a short idle read-timeout on stream calls, and reopen `interactions.get(..., stream=True)` continuously until completion or the UI will freeze behind a long-lived silent socket.
- S2 script review must size the editable textarea from the actual script length instead of hard-coding `rows="3"`; otherwise longer lifestyle scripts look truncated in the UI even when `seed_data.script` is stored intact.
- Batch detail views after `S2_SEEDED` must filter removed posts out of the rendered post list and all stage counters; once a script is rejected, it should disappear from prompt/video/QA/publish screens instead of lingering as excluded noise.
- Batch detail video feedback must render in `S4_SCRIPTED` as well as `S5+`, and in-progress statuses (`submitted/processing/extended_submitted/extended_processing`) must show active generation messaging even when an older `video_url` is already present.
- Any edits in `templates/batches/detail/_post_card.html` must preserve balanced Jinja control blocks (`if/elif/else/endif`) around the video section; one missing `{% endif %}` turns `/batches/{id}` into a hard 500 TemplateSyntaxError.
- S2 script edits must clear `posts.video_prompt_json` as well as updating `seed_data.script`; otherwise a "saved" edit can leave stale prompt state that later generations continue to reuse.
- S2 script review pages must never enable batch-root video polling; `should_poll_videos` must be state-gated to video phases only, or the `hx-trigger="every 5s"` root swap will steal textarea focus while the user is typing.
- Batch video generation and worker reconciliation must promote stale batches through both gates (`S4_SCRIPTED -> S5_PROMPTS_BUILT -> S6_QA`) using active posts only; if video submission is allowed from `S4`, the same path must still self-heal the batch state or completed videos become unapprovable.
- Lifestyle seeding must not dedupe on the fixed 5 template labels alone and batch finalization must verify per-type requested counts; derive lifestyle titles from generated script content and never advance to `S2_SEEDED` if any requested `value/lifestyle/product` quota is still missing.
- Lifestyle 16s output is speed-first: run only one Gemini collection pass, synthesize missing `description` text locally, repair short tier-envelope scripts before persistence, and force-fill any remaining lifestyle slots immediately with tier-valid deterministic copy instead of looping extra generation rounds.
- Lifestyle seeding must persist the primary validated dialog script, not the shorter CTA-stripped `rotation` teaser; otherwise 16s fallback/generated topics can pass PROMPT_2 yet still die in `store_topic_bank_entry(...)` on the pre-persistence tier envelope gate.
- PROMPT_2 lifestyle generation must treat per-template `ValidationError` as retryable and continue to the next template instead of aborting the batch, or a single underlength 32s script will stop mixed `value/lifestyle/product` seeding before product posts are reached.
- PROMPT_3 product validation must follow the product template sentence envelope (8s=1, 16s=3-4, 32s=4-5) instead of the PROMPT_1 32s envelope, or valid product scripts will be rejected during mixed-batch seeding.
- Efficient 32s Veo routes must stay on `8+7+7+7` with time-budgeted rebalance even when the script already has four sentence units; never fall back to `4+7+7+7+7` or the first hop will stay undersized and the tail hop will rush.
- Veo packers must distribute by word budget across the whole script stream and only treat sentence boundaries as soft hints; never lock 16s/32s chaining to sentence count or the first/last hop timing will drift badly.
- PROMPT_2 lifestyle validation must stay permissive at 32s (word floor lowered to 8) because real fallback generations can be short; otherwise mixed batches abort after only the value/product lanes are created.
- PROMPT_2 prompt templates and builder must carry explicit tier word-budget guidance for 8s/16s/32s; if a tier keeps producing underlength scripts, tighten the prompt contract before relaxing validation so batches do not loop forever on short fallback outputs.
- Lifestyle seeding must force-fill any still-missing slots after overlap filtering with deterministic fallback candidates so a batch completes instead of stalling in `S1_SETUP` with 1-2 posts forever.
- Lifestyle seeding retry loops must stay short (currently max 2 collection attempts) before force-fill fallback; optimize for guaranteed output speed, not exhaustive uniqueness search.
- Batch detail HTMX polling must never `outerHTML`-swap the root while an embedded `<video>` is actively playing; guard refreshes on playback state or the native player will reset a valid asset mid-watch and look like a broken short-loop playback failure.
- Batch publish UI must size schedule slots from `posts.length` and gate `Review & Arm Dispatch` on the filled dynamic slot count, not a hard-coded 5-slot Mon->Fri assumption, or one-post batches will never arm.
- `S7_PUBLISH_PLAN` datetime-local inputs must be converted as `Europe/Berlin` wall time instead of browser-local `Date(...)`, and batch confirm must only arm dispatch; do not fake Meta publish success or advance to `S8_COMPLETE` before per-network terminal results are stored.
- Workspace Meta login must not be hidden behind `S7_PUBLISH_PLAN` alone: expose a reusable navbar-level account-connect component outside `detail.html`, keep `/publish/meta/connect` available before scheduling, and only use batch/post context to restore the operator to the right page or post after auth.
- Shared Alpine modals included from `base.html` must declare their own `x-data` root; a store-backed `x-show` outside any Alpine component stays `display:none` even when the store state changes.
- `S5_PROMPTS_BUILT -> S6_QA` must count only active posts in the worker transition gate; if removed or `video_excluded` posts remain in the batch-level total, QA will never unlock even though all eligible videos finished.
- `S5_PROMPTS_BUILT -> S6_QA` cannot rely only on the single post-completion edge; the worker must reconcile stuck `S5` batches every poll cycle or any one-time missed transition leaves fully completed batches stranded in prompts forever.
- Lifestyle topic generation must run through the same `deduplicate_topics(..., threshold=0.35)` gate against `topic_registry` plus already-collected batch topics before `add_topic_to_registry(...)`/`create_post_for_batch(...)`; exact DB uniqueness alone only stops byte-identical triples and lets repetitive lifestyle titles recur across batches.
- Caption generation must fall back to a deterministic local 3-variant bundle after three failed Gemini attempts or malformed marker output; do not hard-fail mixed batches on caption LLM brittleness.
- Mixed-batch lifestyle discovery must synthesize fallback candidates from local templates when dedupe leaves fewer posts than requested; do not hard-fail the batch on sparse Prompt 2 diversity.
- PROMPT_2 lifestyle scripts are often single-sentence lines, so `generate_lifestyle_topics()` must apply the same empty-rotation fallback as `convert_research_item_to_topic()`: if CTA stripping would blank the script, keep the full sentence as `rotation` and derive only a short trailing CTA fragment.
- PROMPT_2 must not accept freeform partial text as success: use structured Gemini JSON for dialog scripts, require the `Beschreibung` payload, and reject any script line that does not end with terminal punctuation before it reaches `seed_data.script` or `dialog_script`.
- PROMPT_2 harvests must never abort on incomplete Gemini JSON/text; if both structured parsing and text salvage fail, synthesize a valid script bank from the stored research dossier and source summary so the topic bank still completes.
- PROMPT_3 product scripts must be normalized to the product envelope (8/16/32 => 16-20/24-34/40-66 words) before storage, and canonical variant validation must use product bounds instead of the value envelope or a valid Gemini response will still fail after parsing.
- Mixed-batch pre-persistence validation must route `post_type="lifestyle"` through Prompt 2 bounds, not Prompt 1 bounds, or 32s lifestyle scripts will be rejected as 54-74-word value drafts after successful generation.
- PROMPT_3 text generation must disable Gemini thinking (`thinkingBudget=0`) and preserve enough `maxOutputTokens` for the full Product/Angle/Script/CTA/Fakten block, or Gemini 2.5 Flash will exhaust the budget on hidden reasoning and truncate the response before the parser sees required fields.
- PROMPT_3 parsing must treat empty `Script:`/`CTA:` headers as multiline sections and recover continuation lines before validating `ProductPromptCandidate`; otherwise a valid Gemini block with the label on one line and the copy on the next will crash mixed batches on the first product.
- Product topic-bank seeding must pass `post_type='product'` into `store_topic_bank_entry(...)`/pre-persistence validation, or 32s product scripts can still be rejected by the legacy Prompt 1 envelope after prompt parsing succeeds.
- TikTok `publish_jobs` writes must set `created_at`/`updated_at` explicitly in app code; depending only on DB defaults or triggers breaks the response contract and local regression tests whenever the real database is not the writer.
- Shared account-hub startup must not hard-import optional provider modules that are not on the deployed branch; guard provider integrations behind fallbacks so a partial provider rollout cannot crash the whole web app at import time.
- Topic discovery cron must live on the Hostinger worker path (`workers/topic_researcher.py`) and never in `vercel.json`; keep Vercel for API routing only, and treat `workers/expansion_worker.py` as optional daily maintenance gated by `video_poller_enable_script_bank_expansion`.
- Topic research deployments must use the unified `workers/topic_worker.py` entrypoint; do not deploy `workers/topic_researcher.py` alone or pending scripts will accumulate without guaranteed audit promotion.
- Coverage-pending batches must not be re-queued on every status poll; gate `coverage_recovery` scheduling with a cooldown or one-shot latch, or a single audited value-family shortage will spin forever in `S1_SETUP` and never give the warm-up worker time to grow bank coverage.
- `S1_SETUP` topic seeding must never depend on fire-and-forget in-memory tasks alone: status/startup recovery must requeue only very recent empty batches with bounded concurrency, or restarts will strand live batches and flood Gemini with stale backlog on boot.
- Family-first topic seeding must never dedupe audited bank suggestions back against `topic_registry`, must clip dossier-derived caption/source-summary metadata before `ResearchAgentItem` persistence, and must let `/batches/{id}/status` requeue empty `coverage_pending` `S1_SETUP` batches once audited family coverage becomes sufficient, or clean-bank coverage will either self-delete, crash during harvest, or strand valid batches indefinitely.
- Meta login scopes must always match the enabled Meta product path: Instagram Login uses `instagram_business_*` permissions, while Facebook Login/Page publishing uses `pages_*` plus `instagram_basic`/`instagram_content_publish`; never mix the two in one OAuth request or Meta will reject the dialog with invalid-scope errors.
- Meta reconnects can surface the linked Instagram target as `connected_instagram_account` instead of `instagram_business_account`; normalize both fields into one publish target and auto-select the sole publishable Page+Instagram pair during callback or S7 will loop on “no manageable Page” after a valid consent flow.
- Batch detail S7 rendering must reuse the publish-slice Meta sanitizer/readiness contract instead of a local token-stripper; if `/publish/accounts/status` is ready but `/batches/{id}` drops `publish_ready/readiness_reason`, the UI will falsely loop on “Connect Meta” while Facebook/Instagram are actually publishable.
- S7 schedule validators must compare UTC-aware datetimes only: browser `toISOString()` values include `Z`, so normalize schedule inputs to `timezone.utc` before future-time checks or `/publish/posts/{id}/schedule` will 500 on naive-vs-aware datetime comparison and block Facebook/Instagram arming.
- HTMX history-restore/back-forward requests must return the full `batches/list.html` or `batches/detail.html` document, not a fragment keyed only off `HX-Request`, or browser back navigation will replay naked HTML with no base styles/scripts.
- HTMX list pages must never self-swap the same `id` that carries `hx-trigger="load"`; load only an inner content node or the page will loop requests and leave the indicator/spinner stuck forever.
- HTMX publish-arm buttons do not send JSON bodies by default; `/publish/batches/{id}/confirm` must accept either JSON or form-encoded `batch_id/confirm` payloads or the UI will 422 even though the direct API call works.
- Keep Python 3.9 import-safe by adding `from __future__ import annotations` before any `A | B` type hints in modules that load during app startup; otherwise a single union annotation can break `app.main` import before tests even reach the hardening slice.
- Meta Page visibility must not rely on `/me/accounts` alone for business-managed assets; if the callback returns zero Pages after a successful consent screen, fall back to `/me/assigned_pages` and keep `business_management` in the login scope so linked Facebook+Instagram assets are not incorrectly marked unpublishable.
- S7 publish cards must pass Alpine one JSON-safe `x-data` options object and must never treat TikTok upload completion or `SEND_TO_USER_INBOX` as post-level `published`; only provider `PUBLISH_COMPLETE` is a terminal live-post state.
- Instagram Reel publishing must treat Meta container processing as a long-running boundary: poll `status_code` long enough for `IN_PROGRESS`, accept both `FINISHED` and `PUBLISHED` as ready states, and include the last seen container status in timeout failures so Facebook-success/Instagram-timeout splits stay actionable.
- TikTok direct-post gating must not be tied only to `S7_PUBLISH_PLAN`; allow direct posting from `S8_COMPLETE` when Facebook/Instagram already finished, or the real publish flow deadlocks locally before any TikTok provider call is made.
- Terminal video completions must strip transient poller lease/error fields (`video_poll_lease_*`, `last_polled_by`, `last_polled_at`, stale provider error keys) before writing `caption_pending`, or a completed Vertex/Sora row can remain visually owned by a dead poller identity and appear stuck.
- TikTok `unaudited_client_can_only_post_to_private_accounts` must be converted at the request boundary into a `ValidationError` before generic third-party handling, or direct-post attempts will keep surfacing opaque `503 third_party_fail` responses even though the actual blocker is provider policy.
- TikTok direct posting can still be rejected by unaudited-client/private-account policy even with `SELF_ONLY`; keep the direct init inside the publish error boundary and surface that 403 as validation with draft-upload guidance instead of a raw 503.
- TikTok OAuth/account persistence must normalize Supabase RPC payloads that return either a dict row or a one-item list; blindly indexing `response.data[0]` after a valid token exchange will crash the callback and leave the workspace stuck in a reconnect loop.
- `topic_registry` must remain script-only: persist one canonical `script` field, derive `rotation`/`cta` only at read/render boundaries, and never reintroduce split storage for registry rows.
- Topic bank selection must rank by `use_count`/`last_used_at` and bump `topic_registry.use_count` when a seed topic is actually harvested; otherwise the cron will keep replaying the same YAML seeds while the bank misleadingly stays at zero usage.
- Never ship tracked fallback secrets in `Settings` or compose files; require explicit env injection and fail fast on missing required keys, and always exclude removed/video_excluded posts from QA/publish counters and advancement checks.
- Stage 3 topic banking must persist exactly 3 variants per tier by storing one script per bucket (`problem_agitate_solution`, `testimonial`, `transformation`) into the `topic_scripts` table.
- Topic hub selection must keep basic-topic launch and generated-topic inspection as separate left-panel modes; never point basic-bank cards at script-drawer endpoints or use coverage-ranked registry lists as the default source for fresh research discovery.
- Topic research launches must schedule from the durable `topic_research_runs` row and recover on startup; never rely on a bare `asyncio.create_task(...)` without a recovery scan or the run will remain stuck in `running` after reloads.
- Topic research warm-ups must use one shared single-seed helper that emits the canonical `8/16/32` variants for both worker and manual hub; never loop tiers around the same seed in separate orchestration paths or the UI will show stale tier semantics and drift from the cron pipeline.
- Topic research warm-ups must fall back to a deterministic local dossier when Gemini Deep Research polling keeps returning provider 500s, then continue the canonical `8/16/32` script warm-up; never fail the manual hub run after the provider boundary when the local synthesis path already exists.
- Synthetic fallback topic warm-ups must force-persist at least one lane when dedupe would otherwise filter every generated lane, or the manual hub will complete without any visible generated topic even though the fallback research succeeded.
- Topic research cron wrappers must heartbeat `topic_research_cron_runs` after each seed and reconcile stale `running` rows on startup/loop using `updated_at` plus active-child checks, or a lost terminal update will leave the wrapper open forever.
- `workers/topic_researcher.py` must normalize hub harvest outputs before iterating them and must always mark a cron run `failed`/`completed` inside the worker when an exception escapes, or a dict-shaped harvest result will become string keys and strand a `running` cron row forever.
- PROMPT_1 fallback synthesis must rebuild `source_summary` from German dossier text whenever the raw lane summary or source titles trigger German-only validation; never trust mixed-language fallback summaries because they can stall the backend deep-research pipeline before Supabase persistence.
- PROMPT_2 fallback synthesis must validate every bucket against the exact tier word bounds and German-only checks before persistence; cleaned lane/topic titles must strip label noise like `Winkel 2` so fallback scripts cannot degrade into short, literal, malformed text.
- Topic-seeding regressions that assert a requested post type stays missing must stub `list_topic_suggestions(...)`; otherwise stored bank suggestions can satisfy quotas and make the test falsely advance to `S2_SEEDED`.
- Prompt builders that consume dossier context must coerce both dict-like and attribute-based objects (for example `TopicData`/`SimpleNamespace`) before formatting; assuming `dict(...)` breaks handler regressions before missing-quota validation can run.
- Deep-research `value` topic banking must be PROMPT_1-only end-to-end (tiered `prompt1_{8,16,32}s.txt`), so `_harvest_seed_topic_to_bank(...)` must not call PROMPT_2 dialog expansion for `post_type=="value"` and should derive the 3 bucket variants from the validated PROMPT_1 script payload.
- Value/product Deep Research must keep Gemini out of JSON entirely on the warm-up path: normalize research prose locally, generate stage-3 scripts as plain text only, and persist canonical `8/16/32` rows per tier so bucket collisions never flatten a lane.
- Canonical PROMPT_1 stage-3 must inject the YAML `hook_bank` into the live value/product prompt path, and the shared script/caption integrity firewall must reject research headings, markdown residue, citations, and dangling parentheticals without false-positive failures on valid separable-verb endings like `ruf ... an!` or `fährt ... mit!`.
- PROMPT_1 stage-3 candidates must enforce tier-specific word envelopes (8:12-15, 16:26-36, 32:54-74) at runtime before persistence, with bounded lane-fact expansion for short outputs and explicit validation failure when still out of range.
- Bank-reuse publish captions must run through cleaned-fact repair before persistence: protect abbreviations like `z.B.`/`u.a.` from sentence splitting, drop unsafe parenthetical examples during caption compression, and prefer generic clean bullets over truncated fact fragments so `seed_data.caption` never stores dangling clauses.
- PROMPT_1 variant expansion must pass the resolved tier profile into `parse_prompt1_response(...)` and re-enforce the tier's minimum/maximum word envelope before `upsert_topic_script_variants(...)`; defaulting to the 8s envelope silently collapses 16s/32s scripts.
- PROMPT_1 lane generation must treat Gemini structured/text failures as recoverable and synthesize a tier-valid fallback item, and `topic_scripts` upserts must key value variants by bucket plus script so identical bucket copies do not collapse the required 3-row tier bank.
- Video pollers must stamp `video_metadata.last_polled_by` and preserve provider status/body on poll failures before marking a post failed; without poller identity and raw Veo error context, concurrent workers can silently race and hide the real failing runtime.
- Veo pollers must treat `done: true` operations with provider `error` payloads as terminal provider failures and persist that message/code directly; otherwise internal Veo failures get mislabeled as missing download URIs.
- Caption generation must emit one validated `caption_bundle` per post with enforced paragraph-family structure (`short_paragraph`, `medium_bullets`, `long_structured`), persist the selected variant into `posts.publish_caption`, and fall back deterministically whenever Gemini drops line breaks, truncates list blocks, or breaks the expected schema.
- Deterministic caption fallbacks must clip injected topic/context text to the target family bounds before validation; long researched summaries may not overflow short/medium/long bucket limits or crash live preview generation.
- Caption fallback synthesis must always return all three validated families (never one-lane only) and must keep each template inside its exact char bucket so malformed Gemini JSON can never crash post creation or caption repair jobs.
- Caption generation must never bubble provider exceptions to callers; on any Gemini JSON/text/runtime failure, synthesize a validated 3-variant bundle and return the chosen caption instead of failing the batch.
- Veo video extension must use the REST `predictLongRunning` `video.uri` path only; the Gemini SDK extension path currently rejects valid `9:16` inputs with `INVALID_ARGUMENT`, so do not route portrait extension jobs through the SDK.
- Topics hub desktop panels must keep a shared fixed height (`lg:h-[80vh]`) with internal scrolling; never let launch/active-run HTMX swaps resize the right panel during deep-research progress.
- PROMPT_1 research hooks must never be copied into `seed_data.caption` or `publish_caption`; keep them in `research_caption`/description only and let the validated caption bundle own the published TikTok caption, with `value` posts defaulting to medium/long families.
- Canonical research subjects must be carried separately from lane/dossier display titles (`canonical_topic` vs `research_title`/`topic_title`); caption prompting and title-copy detection must use the canonical subject only or lane labels like `Forschungsdossier` will leak into published captions.
- Topic/batch harvest must derive `SeedData` locally from normalized dossier/lane payloads and coerce namespace-like fixtures safely; never reintroduce a second Gemini JSON seed-extraction call, because that extra structured boundary caused manual hub runs to fail with `Gemini structured generation failed`.
- Topic hub freshness cues must be derived from `last_harvested_at` with a short recency window and sorted ahead of older rows; never add a separate persistence flag or a second discovery surface for "new" topics, or operators lose the landing point for freshly completed research.
- Value/product warm-up must research exactly 3 unique seeds per run, persist one canonical `topic_scripts.bucket="canonical"` row set for `8/16/32` per accepted lane, and skip identical registry/tier or dossier/lane/tier scripts instead of cloning bucket copies.
- Value-bank batch discovery must pass the batch tier into every `create_post_for_batch(...)` call, including stored-suggestion reuse branches, or `seed_data.target_length_tier` drops out of the final post payload and the live bank E2E check fails despite correct canonical coverage.
- PROMPT_1 long-form Stage-3 assembly must sanitize dossier-derived summaries/facts before fallback or envelope expansion, strip labels like `Demografische Dringlichkeit` / `Zentrale Erkenntnisse` plus markdown/artifact residue, and reject any script that still looks truncated or research-note contaminated before `topic_scripts` persistence.
- Veo extended pollers must treat provider `400 INVALID_ARGUMENT` `"Input video must be a video that was generated by VEO that has been processed."` as a transient extension-readiness boundary, keep the post in `extended_processing`, and retry with backoff instead of failing the chain on the first completed-base poll.
- Hostinger Docker redeploys must not rely on repo-URL defaults or `updateProject` alone: GitHub project creation resolves against `master`, `updateProject` only recreates existing images, and production Veo fixes reach the worker only when the project is rebuilt from an explicit git build-context branch containing the patched `app/adapters/veo_client.py` and `workers/video_poller.py`.
- Production/runtime requirements must not pin `google-genai` unless live code imports it: this repo uses REST for Gemini/VEO, and the unused SDK's `anyio>=4.8` constraint conflicts with FastAPI `0.104.1`/`anyio<4`, making Hostinger image rebuilds fail before startup.
- Auth OTP UI must never hard-code 6-slot widths or fixed flex row spacing; always read `AUTH_OTP_CODE_LENGTH`/`otp_code_length` from settings and render the digits in a fluid grid so Supabase 8-digit codes stay inside the login card on desktop and mobile.
- Local auth bypass must key off the actual request host (`localhost`/`127.0.0.1`) instead of an env flag alone, or local runs will still burn Supabase OTP quota even when development mode is intended to skip auth.
- Never create the Supabase client during application startup; a bad API key or missing secret must degrade health checks instead of crashing the lifespan and bricking the deployment.
- VEO submissions must use the durable quota ledger before any provider call: reserve the full chain budget up front, consume one unit only after each accepted operation id, release unused units on terminal failure, and freeze Veo until next Pacific reset after any unexpected `429` so partial chains cannot burn spend after quota is gone.
- Development multi-key tests may bypass the local VEO quota guard only through the explicit `veo_disable_local_quota_guard` setting; never delete the ledger, just short-circuit freeze/reserve/consume/release checks when the bypass is enabled.
- Topic-bank selection for batch seeding must prioritize diversity (`use_count` ascending + oldest `last_used_at` first) and collapse same-family titles before truncation; never rank by highest `use_count` first or hot topics will self-reinforce and repeat across multi-post generations.
- Blog generation must treat unlabeled LLM prose as malformed input: synthesize intro/sections/conclusion from dossier fallback, keep `preview_text` short for card teasers, and republish the same Webflow item in place instead of creating duplicate posts.
- Gemini blog image generation must map nickname values like `nanobanana-2` to Google’s real image model ids and send `generationConfig.imageConfig` for a square image, or the UI `Generate Image` step will fail with provider 404s.
- Batch prompt editors must never inline full prompt JSON into Alpine `x-data`; pass it via a `data-*` attribute and `JSON.parse()` it in component init, or browser expressions will break on quoted prompt text and kill the modal before save.
- VEO model selection must match the active API surface: keep `veo-3.1-generate-preview` on the Gemini Developer API endpoint, and only switch to `veo-3.1-generate-001` when the adapter is migrated to the Vertex AI publisher endpoint.
- VEO quota accounting must finalize every successful chain with the full request count already consumed in the reservation row; successful 16s/32s completions may not leave `released_units` holding actually-sent extension hops or the Pacific-day snapshot will undercount spend and hide rate-limit pressure.
- Efficient-long-route experiments must persist the active profile flag on submit and log the real hop target at poll time; never assume 32s still means four extension hops / five total requests once the 8s-base flag is active.
- Local development startup must skip stalled-batch recovery unless explicitly re-enabled, or fake Supabase credentials will still trigger boot-time DB queries and prevent the server from staying up.
- Any rollout that writes `caption_*` or `extended_*` post video states must ship the matching `posts_video_status_check` migration to the live Supabase project before worker deploy, or completed uploads will fail at persistence and strand valid videos off-UI.
- Topic scripts are one-and-done for reuse: `mark_topic_script_used()` is publication history only, and every selector/batch path must hard-exclude `topic_scripts.use_count > 0` so family reuse happens through fresh variants instead of reposting the exact same script.
- Topic pre-persistence normalization must treat `title`/`topic` as plain metadata (no sentence-splitting) so dash-free titles do not collapse to blank, and 8s scripts must stay inside the 14-18 word envelope before persistence.
- Pre-persistence tier gates must mirror the published audit contract for 8s/16s/32s; never tighten 8s above 14-18 words or valid live scripts will fail at `store_topic_bank_entry(...)` even after passing generation.
- Batch discovery must treat "created at least one post" as partial success and must not raise a terminal `ValidationError` merely because requested quota was not fully met after persistence, or cron runs will return 500 despite writing posts.
- Prompt 3 sentence counting must ignore decimal facts like `2.990 mm`; use a punctuation-aware splitter instead of `split(".")` or valid product scripts will be rejected as two sentences.
- Prompt edit saves must bootstrap from `seed_data` whenever batch detail is rendering a synthesized preview with `video_prompt_json = null`; otherwise the UI exposes an editable prompt modal while `PATCH /posts/{id}/prompt` hard-422s with "Build the prompt before editing it." on legacy or not-yet-built rows.
- Audit JSON must be structured-first with one text-repair fallback before reject; never short-circuit Gemini parse failures directly into `audit_response_parse_error` when the JSON schema path can recover the row.
- Topic warm-up must force one fallback lane when every researched lane dedupes away; returning zero persisted lanes from a successful dossier is a silent cron failure that must be prevented by final fallback persistence.
- `app/features/topics/prompt_data/hook_bank.yaml` must keep `negative_examples` indented as a nested YAML sequence; a flat list here makes `yaml.safe_load()` explode, breaks `variant_expansion`, and stalls the daily script-growth worker even when the main web app stays healthy.
- `/topics/cron/discover` must never hard-500 on per-batch discovery failures; it should return seeded/failed batch details so the autonomous daily cron keeps reporting progress even when some S1 batches are empty or temporarily unseedable.
- Prompt rebuilds must preserve operator-edited `video_prompt_json` sections when regenerating from `seed_data`; rebuilding may refresh derived fields, but it must not overwrite an expanded character prompt or edited Veo prompt with canned defaults.
- Google application credentials must never be embedded inline in tracked compose files; load `GOOGLE_APPLICATION_CREDENTIALS_JSON` from local env/secret injection only so video generation keeps working without publishing secrets.
- Vertex AI batch/single video submissions must pass the UI-selected `model` into `submit_text_video`/`submit_image_video` and persist both `video_metadata.requested_model` and `video_metadata.provider_model`; otherwise Fast/Lite requests silently degrade to regular `veo-3.1-generate-001` while the UI only shows an ambiguous operation id.
- Video resubmits and healthy poll-progress updates must clear transient `video_metadata.error`/poller failure fields before persisting `submitted/processing`; otherwise a recovered Vertex run keeps displaying a stale ADC/provider error and looks broken even while polling normally.
- Shared Supabase video rows must carry a deployment-specific `video_metadata.poller_scope` derived from `APP_URL`/host, and pollers must reject rows from other scopes before claiming a lease; `ENVIRONMENT` alone is insufficient because a live container mis-set to `development` can still steal localhost jobs and keep rewriting bogus ADC failures.
